<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BungaFloryn"
        android:usesCleartextTraffic="true">

        <!-- FileProvider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Reminder BroadcastReceiver -->
        <receiver
            android:name=".utils.ReminderReceiver"
            android:exported="false" />

        <!-- SPLASH SCREEN sebagai halaman utama -->
        <activity
            android:name=".Home.pertemuan_6.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding -->
        <activity
            android:name=".Onboarding.OnboardingActivity"
            android:exported="false" />

        <!-- Auth (Login RuangKu) - sejajar MainActivity -->
        <activity
            android:name=".AuthActivity"
            android:exported="false" />

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Base Activity dengan BottomNavigation -->
        <activity
            android:name=".BaseActivity"
            android:exported="false" />

        <!-- Pertemuan 3 -->
        <activity
            android:name=".Home.pertemuan_3.WelcomeActivity"
            android:exported="false" />

        <!-- Pertemuan 4 -->
        <activity
            android:name=".Home.pertemuan_4.CustomActivity1"
            android:exported="false" />
        <activity
            android:name=".Home.pertemuan_4.CustomActivity2"
            android:exported="false" />

        <!-- Pertemuan 5 -->
        <activity
            android:name=".Home.pertemuan_5.FifthActivity"
            android:exported="false" />
        <activity
            android:name=".Home.pertemuan_5.WebViewActivity"
            android:exported="false" />

        <!-- Pertemuan 2 -->
        <activity
            android:name=".Home.pertemuan_2.MainActivity"
            android:exported="false" />

        <!-- Main -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Pertemuan 9 -->
        <activity
            android:name=".Home.pertemuan_9.NinthActivity"
            android:exported="false" />

        <!-- Pertemuan 10 -->
        <activity
            android:name=".Home.pertemuan_10.TenthActivity"
            android:exported="false" />

        <!-- Pertemuan 13 -->
        <activity
            android:name=".Home.pertemuan_13.ThirteenthActivity"
            android:exported="false" />

        <!-- Room: Note -->
        <activity
            android:name=".Note.NoteFormActivity"
            android:exported="false" />

        <!-- Room: Warga -->
        <activity
            android:name=".Warga.WargaFormActivity"
            android:exported="false" />

    </application>

</manifest>